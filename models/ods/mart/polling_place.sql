-- 投票所一覧（期日前投票所を含む、自治体標準ODS、区市町村横断）
select
    municipality_code,
    organization_name,
    facility_id,
    polling_place_id,
    voting_district_number,
    name,
    name_kana,
    name_en,
    description,
    address,
    prefecture,
    city,
    ward,
    town,
    town_id,
    street_number,
    building_name,
    postal_code,
    phone_number,
    lat,
    lon,
    crs,
    crs_code,
    polling_place_type,
    voting_district,
    election_type,
    notes,
    extras,
    org_code,
    org_title,
    package_id,
    source_url
from {{ ref('stg_polling_place') }}
