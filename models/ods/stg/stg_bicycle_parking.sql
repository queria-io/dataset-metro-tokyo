{# 自転車駐車場のステージング。緯度経度・収容台数を数値化し、開設日を DATE に正規化する。 #}

select
    municipality_code,
    facility_id,
    organization_name,
    name,
    name_kana,
    name_en,
    name_alias,
    poi_code,
    address,
    prefecture,
    city,
    building_name,
    postal_code,
    phone_number,
    try_cast(lat as double) as lat,
    try_cast(lon as double) as lon,
    {{ ods_geo_columns() }},
    indoor_outdoor,
    try_cast(capacity as integer) as capacity,
    nearest_station,
    available_days,
    start_time,
    end_time,
    available_notes,
    fee_type,
    fee,
    fee_details,
    payment_type,
    {{ ods_date('opened_date') }} as opened_date,
    description,
    url,
    notes,
    _extras as extras,
    _package_id as package_id,
    _resource_id as resource_id,
    _org_code as org_code,
    _org_title as org_title,
    _source_url as source_url
from {{ ref('raw_bicycle_parking') }}
