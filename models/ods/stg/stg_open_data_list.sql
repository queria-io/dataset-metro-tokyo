{# オープンデータ一覧のステージング。日付列を DATE に、ファイルサイズを数値に、
   API対応有無を BOOLEAN に正規化する。施設一覧ではないため緯度経度の列は持たない。 #}

select
    municipality_code,
    dataset_id,
    organization_name,
    dataset_title,
    dataset_subtitle,
    dataset_description,
    keywords,
    category,
    universal_menu,
    {{ ods_date('published_date') }} as published_date,
    {{ ods_date('dataset_updated') }} as dataset_updated,
    version,
    language,
    dataset_url,
    update_frequency,
    conforms_to,
    related_documents,
    provenance,
    spatial_coverage,
    temporal_coverage,
    contact_name,
    contact_email,
    contact_phone,
    contact_extension,
    contact_form_url,
    contact_notes,
    dataset_notes,
    file_title,
    file_access_url,
    file_download_url,
    file_description,
    file_format,
    file_license,
    file_status,
    try_cast(file_size as bigint) as file_size,
    {{ ods_date('file_published_date') }} as file_published_date,
    {{ ods_date('file_updated') }} as file_updated,
    file_terms,
    file_related_documents,
    file_language,
    file_conforms_to,
    {{ ods_flag('api_available') }} as api_available,
    _extras as extras,
    _package_id as package_id,
    _resource_id as resource_id,
    _org_code as org_code,
    _org_title as org_title,
    _source_url as source_url
from {{ ref('raw_open_data_list') }}
