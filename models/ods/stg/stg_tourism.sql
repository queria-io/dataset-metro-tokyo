{# 観光施設のステージング。緯度経度・基本料金を数値化する。 #}

select
    municipality_code,
    facility_id,
    name,
    name_kana,
    name_en,
    poi_code,
    address,
    prefecture,
    city,
    building_name,
    postal_code,
    try_cast(lat as double) as lat,
    try_cast(lon as double) as lon,
    corporate_number,
    available_days,
    start_time,
    end_time,
    available_notes,
    try_cast(fee as integer) as fee,
    fee_details,
    description,
    description_en,
    access,
    parking,
    barrier_free,
    contact_name,
    contact_phone,
    contact_extension,
    image,
    image_license,
    url,
    notes,
    _extras as extras,
    _package_id as package_id,
    _resource_id as resource_id,
    _org_code as org_code,
    _org_title as org_title,
    _source_url as source_url
from {{ ref('raw_tourism') }}
